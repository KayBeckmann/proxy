# Tor Hidden Service Konfiguration
# ================================

# Präfix für die .onion-Adresse (Vanity Address)
# Nur Kleinbuchstaben a-z und Ziffern 2-7 erlaubt (Base32)
# Je länger der Präfix, desto länger dauert die Generierung:
#   3 Zeichen: ~Sekunden
#   4 Zeichen: ~Minuten
#   5 Zeichen: ~Stunden
#   6 Zeichen: ~Tage
# Leer lassen für zufällige Adresse
VANITY_PREFIX=

# Lokaler Port des Dienstes, der weitergeleitet werden soll
# Dies ist der Port auf dem Host, auf dem dein Dienst läuft
LOCAL_PORT=8080

# Port über den der Dienst im Tor-Netzwerk erreichbar sein soll
# Standard ist 80 für HTTP
ONION_PORT=80
